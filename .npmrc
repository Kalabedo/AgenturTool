auto-install-peers=true
strict-peer-dependencies=false

# `pnpm deploy` beim Verpacken: Ohne diesen Schalter löst es die
# Workspace-Pakete nicht in echte Verzeichnisse auf, und electron-builder
# steht vor Symlinks, denen es nicht folgt.
inject-workspace-packages=true
