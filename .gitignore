node_modules/
dist/
coverage/
*.tsbuildinfo

# Verpackung: der vorbereitete Baum und die fertigen Pakete.
# `build/` steht hier absichtlich nicht: apps/desktop/build enthält die
# Entitlements, ohne die die signierte Anwendung nicht startet.
apps/desktop/paket/
apps/desktop/release/
apps/desktop/build/appx/

# Laufzeitdaten: Datenbank, Assets, erzeugte PDFs
data/

# Secrets
.env
.env.local

.DS_Store
*.log
