name: Desktop-Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Tag und Hauptzweig prüfen
        run: |
          node apps/desktop/scripts/release-preflight.mjs
          git fetch origin main:refs/remotes/origin/main --no-tags
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main || {
            echo "Der Release-Commit gehört nicht zu origin/main." >&2
            exit 1
          }
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: xvfb-run -a pnpm verify
        env:
          ELECTRON_NO_SANDBOX: 'true'

  macos:
    needs: verify
    environment: release
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            arch: arm64
          - os: macos-15-intel
            arch: x64
    name: Signieren (macOS ${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      # `notarytool` nimmt den App-Store-Connect-Schlüssel nur als Datei
      # entgegen (`--key`), und electron-builder reicht `APPLE_API_KEY`
      # unverändert dorthin weiter. Das Geheimnis enthält deshalb den
      # PEM-Inhalt der `.p8`, und hier entsteht daraus kurzzeitig eine Datei
      # neben dem Lauf — nicht im Arbeitsbaum, damit sie in kein Paket gerät.
      - name: Notarisierungsschlüssel bereitlegen
        env:
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
        run: |
          set -euo pipefail
          umask 077
          printf '%s\n' "$APPLE_API_KEY_P8" > "$RUNNER_TEMP/AuthKey.p8"

      - name: Signieren, notarisieren und DMG bauen
        run: pnpm --filter @agentur-tool/desktop paket --release
        env:
          CSC_LINK: ${{ secrets.MAC_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
          APPLE_API_KEY: ${{ runner.temp }}/AuthKey.p8
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}

      # Der Runner wird ohnehin verworfen. Der Schlüssel liegt trotzdem nur
      # so lange, wie er gebraucht wird — auch wenn der Bau abbricht.
      - name: Notarisierungsschlüssel entfernen
        if: always()
        run: rm -f "$RUNNER_TEMP/AuthKey.p8"

      - name: Signatur, Notarisierung und Laufzeit prüfen
        shell: bash
        run: |
          set -euo pipefail
          DMG_COUNT="$(find apps/desktop/release -maxdepth 1 -name 'AgenturTool-*-${{ matrix.arch }}.dmg' | wc -l | tr -d ' ')"
          test "$DMG_COUNT" = 1
          DMG="$(find apps/desktop/release -maxdepth 1 -name 'AgenturTool-*-${{ matrix.arch }}.dmg' -print -quit)"
          hdiutil verify "$DMG"

          MOUNT_POINT="$RUNNER_TEMP/agentur-tool-release-dmg"
          DATA_DIR="$RUNNER_TEMP/agentur-tool-release-smoke"
          mkdir -p "$MOUNT_POINT" "$DATA_DIR"
          hdiutil attach "$DMG" -readonly -nobrowse -mountpoint "$MOUNT_POINT" >/dev/null
          trap 'hdiutil detach "$MOUNT_POINT" >/dev/null || true' EXIT

          APP="$(find "$MOUNT_POINT" -maxdepth 1 -name '*.app' -print -quit)"
          test -n "$APP"
          codesign --verify --deep --strict --verbose=2 "$APP"
          spctl --assess --type execute --verbose=2 "$APP"
          xcrun stapler validate "$APP"
          node apps/desktop/scripts/rauchprobe.mjs "$APP" --data-dir "$DATA_DIR"
          node apps/desktop/scripts/rauchprobe.mjs "$APP" --data-dir "$DATA_DIR" --reopen

      - uses: actions/upload-artifact@v4
        with:
          name: signed-macos-${{ matrix.arch }}
          path: apps/desktop/release/AgenturTool-*-${{ matrix.arch }}.dmg
          if-no-files-found: error

  windows:
    needs: verify
    environment: release
    name: Signieren (Windows x64)
    runs-on: windows-2025
    timeout-minutes: 45
    env:
      # Die Fassung von CodeSignTool steht fest, damit ein Release nicht
      # davon abhängt, was der Aussteller gerade als „latest" führt. Zum
      # Anheben: Fassung auf der Releaseseite von SSLcom/CodeSignTool
      # nachsehen, hier eintragen, einmal durchlaufen lassen.
      CODE_SIGN_TOOL_VERSION: v1.3.0
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      # CodeSignTool ist ein Java-Programm. Auf dem Runner liegt zwar eine
      # Java-Fassung vor, aber welche, ist nicht zugesagt.
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      # Signiert wird über das Cloud-HSM des Ausstellers: Seit dem
      # 1. Juni 2023 verlangen die Baseline Requirements des
      # CA/Browser-Forums, dass der private Schlüssel auf zertifizierter
      # Hardware entsteht und nicht-exportierbar bleibt. Eine PFX zum
      # Herunterladen, wie `CSC_LINK` sie erwartet, gibt es deshalb nicht
      # mehr. Den Aufruf macht `apps/desktop/scripts/signieren-windows.mjs`
      # als Haken von electron-builder; hier wird nur das Werkzeug
      # bereitgelegt.
      - name: CodeSignTool bereitlegen
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $ErrorActionPreference = 'Stop'
          $release = gh api "repos/SSLcom/CodeSignTool/releases/tags/$env:CODE_SIGN_TOOL_VERSION" | ConvertFrom-Json
          $asset = $release.assets | Where-Object { $_.name -match '(?i)windows.*\.zip$' } | Select-Object -First 1
          if (-not $asset) {
            throw "CodeSignTool $env:CODE_SIGN_TOOL_VERSION enthält kein Windows-Paket."
          }

          $zip = Join-Path $env:RUNNER_TEMP 'CodeSignTool.zip'
          $dir = Join-Path $env:RUNNER_TEMP 'codesigntool'
          Invoke-WebRequest -Uri $asset.browser_download_url -OutFile $zip
          Expand-Archive -Path $zip -DestinationPath $dir -Force

          # Je nach Fassung liegt das Skript unmittelbar im Archiv oder in
          # einem Unterverzeichnis mit der Versionsnummer im Namen.
          $bat = Get-ChildItem -Path $dir -Filter 'CodeSignTool.bat' -Recurse | Select-Object -First 1
          if (-not $bat) { throw 'CodeSignTool.bat liegt nicht im entpackten Paket.' }
          "CODE_SIGN_TOOL_PATH=$($bat.DirectoryName)" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Signieren und NSIS-Installer bauen
        run: pnpm --filter @agentur-tool/desktop paket --release
        env:
          SSL_COM_USERNAME: ${{ secrets.SSL_COM_USERNAME }}
          SSL_COM_PASSWORD: ${{ secrets.SSL_COM_PASSWORD }}
          SSL_COM_CREDENTIAL_ID: ${{ secrets.SSL_COM_CREDENTIAL_ID }}
          SSL_COM_TOTP_SECRET: ${{ secrets.SSL_COM_TOTP_SECRET }}

      - name: Signaturen, Installation und Laufzeit prüfen
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $installers = @(Get-ChildItem 'apps/desktop/release/AgenturTool-*-x64.exe')
          if ($installers.Count -ne 1) { throw "Erwartete genau einen Installer, fand $($installers.Count)." }

          $unpacked = 'apps/desktop/release/win-unpacked/AgenturTool.exe'
          foreach ($file in @($installers[0].FullName, $unpacked)) {
            $signature = Get-AuthenticodeSignature $file
            if ($signature.Status -ne 'Valid') {
              throw "Ungültige Authenticode-Signatur für $file: $($signature.Status)"
            }
            # Ohne Zeitstempel gilt die Signatur nur, solange das Zertifikat
            # gilt — und ein Cloud-Zertifikat lebt höchstens 460 Tage. Ein
            # ausgelieferter Installer wäre dann eines Tages ungültig, ohne
            # dass sich an ihm etwas geändert hat.
            if (-not $signature.TimeStamperCertificate) {
              throw "Signatur ohne RFC-3161-Zeitstempel für $file."
            }
          }

          $installDir = Join-Path $env:RUNNER_TEMP 'AgenturTool-Installation'
          $dataDir = Join-Path $env:RUNNER_TEMP 'AgenturTool-Smoke'
          New-Item -ItemType Directory -Force -Path $dataDir | Out-Null
          $install = Start-Process -FilePath $installers[0].FullName -ArgumentList '/S', "/D=$installDir" -Wait -PassThru
          if ($install.ExitCode -ne 0) { throw "Installer endete mit Code $($install.ExitCode)." }

          $app = Join-Path $installDir 'AgenturTool.exe'
          $installedSignature = Get-AuthenticodeSignature $app
          if ($installedSignature.Status -ne 'Valid') {
            throw "Installierte Anwendung ist nicht gültig signiert: $($installedSignature.Status)"
          }
          node apps/desktop/scripts/rauchprobe.mjs $app --data-dir $dataDir
          if ($LASTEXITCODE -ne 0) { throw 'Erste Rauchprobe fehlgeschlagen.' }
          node apps/desktop/scripts/rauchprobe.mjs $app --data-dir $dataDir --reopen
          if ($LASTEXITCODE -ne 0) { throw 'Rauchprobe nach Neustart fehlgeschlagen.' }

          $uninstaller = Join-Path $installDir 'Uninstall AgenturTool.exe'
          $uninstall = Start-Process -FilePath $uninstaller -ArgumentList '/S' -Wait -PassThru
          if ($uninstall.ExitCode -ne 0) { throw "Deinstallation endete mit Code $($uninstall.ExitCode)." }

      - uses: actions/upload-artifact@v4
        with:
          name: signed-windows-x64
          path: apps/desktop/release/AgenturTool-*-x64.exe
          if-no-files-found: error

  draft:
    needs: [macos, windows]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # Für das Feed-Skript; die übrigen Schritte brauchen den Quellbaum nicht.
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/download-artifact@v4
        with:
          pattern: signed-*
          path: dist
          merge-multiple: true
      - name: Prüfsummen erzeugen
        run: |
          cd dist
          sha256sum AgenturTool-*.dmg AgenturTool-*.exe > SHA256SUMS

      # Die Datei, die die installierte Anwendung später abfragt. Sie liegt
      # dem Release bei, damit veröffentlichte Prüfsummen und Feed aus
      # demselben Lauf stammen; auf die Website kommt sie zuletzt und erst,
      # wenn die Pakete dort schon liegen (docs/RELEASE.md).
      - name: Updatefeed erzeugen
        env:
          TAG: ${{ github.ref_name }}
        run: |
          node apps/desktop/scripts/updatefeed.mjs --dir dist --version "${TAG#v}"
          cat dist/updates.json
      - name: Entwurfs-Release anlegen oder aktualisieren
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if IS_DRAFT="$(gh release view "$TAG" --json isDraft --jq .isDraft 2>/dev/null)"; then
            if [ "$IS_DRAFT" != true ]; then
              echo "Release $TAG ist bereits veröffentlicht und wird nicht überschrieben." >&2
              exit 1
            fi
          else
            gh release create "$TAG" --draft --generate-notes --title "AgenturTool $TAG" --verify-tag
          fi
          gh release upload "$TAG" dist/* --clobber
